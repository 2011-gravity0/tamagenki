diff a/public/main.js b/public/main.js	(rejected hunks)
@@ -9 +9 @@ export const pushSetting = (user, signUp) => {
-  if (Object.values(user).some((val) => val === true)) anyReminder = true
+  if (Object.values(user).some(val => val === true)) anyReminder = true
@@ -19 +19 @@ export const pushSetting = (user, signUp) => {
-      swRegistration.pushManager.getSubscription().then((subscription) => {
+      swRegistration.pushManager.getSubscription().then(subscription => {
@@ -39 +39 @@ export const pushSetting = (user, signUp) => {
-    Notification.requestPermission((status) => {
+    Notification.requestPermission(status => {
@@ -51 +51 @@ export const pushSetting = (user, signUp) => {
-          applicationServerKey: applicationServerKey,
+          applicationServerKey: applicationServerKey
@@ -53 +53 @@ export const pushSetting = (user, signUp) => {
-        .then((subscription) => {
+        .then(subscription => {
@@ -60 +60 @@ export const pushSetting = (user, signUp) => {
-        .catch((err) => {
+        .catch(err => {
@@ -74 +74 @@ export const pushSetting = (user, signUp) => {
-      .then((subscription) => {
+      .then(subscription => {
@@ -80 +80 @@ export const pushSetting = (user, signUp) => {
-      .catch((err) => {
+      .catch(err => {
@@ -119 +119 @@ export const pushSetting = (user, signUp) => {
-    const padding = '='.repeat((4 - (base64String.length % 4)) % 4)
+    const padding = '='.repeat((4 - base64String.length % 4) % 4)
@@ -139 +139 @@ export const pushSetting = (user, signUp) => {
-        .then((swReg) => {
+        .then(swReg => {
@@ -148 +148 @@ export const pushSetting = (user, signUp) => {
-        .catch((err) => {
+        .catch(err => {
